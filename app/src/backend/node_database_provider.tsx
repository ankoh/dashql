import React from 'react';

type DatabaseProps = {
    children: React.ReactElement | React.ReactElement[];
};

export const NodeDatabaseProvider: React.FC<DatabaseProps> = (props: DatabaseProps) => {
    return <div></div>;
};
