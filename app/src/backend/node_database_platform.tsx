import React from 'react';

type PlatformProps = {
    children: React.ReactElement | React.ReactElement[];
};

export const NodeDatabasePlatform: React.FC<PlatformProps> = (props: PlatformProps) => {
    return <div></div>;
};
