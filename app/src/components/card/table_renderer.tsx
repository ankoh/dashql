import * as React from 'react';

interface Props {
    statementId: number;
    editable?: boolean;
}

export const TableRenderer: React.FunctionComponent<Props> = (props: Props) => {
    return <div>TableRenderer</div>;
};
