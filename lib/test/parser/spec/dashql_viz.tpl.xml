<test name="dashql_viz_full">
    <input>
        VISUALIZE weather_avg USING STACKED BAR (
            position = (r = 0, c = 0, w = 4, h = 15)
        ), LINE (
        ), SCATTER (
        ), XAXIS (
        ), YAXIS (
        );
    </input>
</test>
