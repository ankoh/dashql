name: Branch

on:
    push:
        branches-ignore:
            - 'main'
            - 'renovate/*'
            - 'akohn-publish'
            - 'akohn-tauri'

jobs:
    build:
        name: Build
        uses: ./.github/workflows/build.yml
        with:
            commit: ${{ github.sha }}
            signed: false
        secrets: inherit
