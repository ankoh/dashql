name: Branch

on:
    push:
        branches-ignore:
            - 'main'
            - 'akohn-publish'
            - 'akohn-tauri'

jobs:
    build:
        name: Build
        uses: ./.github/workflows/build_trusted.yml
        with:
            commit: ${{ github.sha }}
            signed: false
        secrets: inherit
