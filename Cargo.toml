# Root workspace for dashql-compute only (used by Bazel crate_universe).
# Paths are relative to repo root so crate_universe can resolve them.
# Native packages use cargo/native/Cargo.toml.
[workspace]
resolver = "2"
members = ["packages/dashql-compute"]

[profile.release]
strip = true
lto = true
opt-level = "s"
