# https://www.buildbuddy.io/docs/config-all-options/

app:
  build_buddy_url: "https://buildbuddy.dashql.app"

storage:
  disk:
    root_directory: /opt/buildbuddy/data/blob

cache:
  max_size_bytes: 53687091200 # 50 GiB
  disk:
    root_directory: /opt/buildbuddy/data/cache

ssl:
  enable_ssl: false

port: 8080
grpc_port: 1985
