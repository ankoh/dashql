{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "dashql/app_notebook.json",
  "title": "NotebookData",
  "description": "Version 2 notebook data structure (dashql-notebook.json)",
  "type": "object",
  "properties": {
    "formatVersion": {
      "type": "integer",
      "enum": [2],
      "description": "Notebook persistence format. Only version 2 is supported."
    },
    "notebookId": {
      "type": "string",
      "description": "Unique notebook identifier (UUID v4). This is the authoritative identity; the storage location prefix is display-only."
    },
    "notebookPath": {
      "type": "string",
      "description": "Display-only path (e.g., \"opfs://notebooks/<uuid>\" or \"fs://<dir>\"). Not authoritative and may be absent on newly written notebooks."
    },
    "storageType": {
      "type": "string",
      "enum": ["opfs", "native"],
      "description": "Where this notebook's files live. Defaults to \"opfs\" when omitted."
    },
    "nativePath": {
      "type": "string",
      "description": "Absolute directory holding this notebook's files when storageType is \"native\"."
    },
    "name": {
      "type": "string",
      "description": "User-supplied notebook name shown as the primary label in the notebook bar and selector. Absent (or empty) means the user never named the notebook, and the UI falls back to the display path."
    },
    "mainDatabase": {
      "$ref": "#/definitions/AttachedDatabase",
      "description": "Database used for notebook analysis, catalog operations, and user query execution."
    },
    "attachedDatabases": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/AttachedDatabase"
      },
      "description": "Additional databases attached to the main database. The application's internal compute database is never persisted here."
    },
    "metadata": {
      "$ref": "#/definitions/NotebookMetadata",
      "description": "The notebook metadata"
    },
    "lastModified": {
      "type": "string",
      "format": "date-time",
      "description": "When was last modified"
    },
    "lastAccessed": {
      "type": "string",
      "format": "date-time",
      "description": "When was last accessed"
    }
  },
  "required": ["formatVersion", "notebookId", "mainDatabase", "attachedDatabases", "metadata"],
  "additionalProperties": false,
  "definitions": {
    "AttachedDatabase": {
      "type": "object",
      "description": "A persisted database attached to the notebook",
      "properties": {
        "databaseId": {
          "type": "string",
          "description": "Stable database/connection identifier (UUID)"
        },
        "params": {
          "$ref": "connection.json#/definitions/ConnectionParams",
          "description": "Database connection parameters"
        }
      },
      "required": ["databaseId", "params"],
      "additionalProperties": false
    },
    "NotebookOriginType": {
      "type": "string",
      "enum": ["FILE", "LOCAL", "HTTP"],
      "description": "The origin type of the notebook"
    },
    "NotebookMetadata": {
      "type": "object",
      "description": "Notebook metadata",
      "properties": {
        "originType": {
          "$ref": "#/definitions/NotebookOriginType",
          "description": "The origin type"
        },
        "originalFileName": {
          "type": "string",
          "description": "The original file name"
        },
        "originalHttpUrl": {
          "type": "string",
          "description": "The original HTTP URL"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "When was created"
        }
      },
      "additionalProperties": false
    }
  }
}
