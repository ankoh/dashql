syntax = "proto3";

package dashql;

/// Detailed Error
message DetailedError {
    /// The error message
    string message = 1;
    /// Additional error details
    map<string, string> details = 2;
}
