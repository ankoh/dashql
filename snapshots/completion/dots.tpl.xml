<completion-snapshots>
    <!-- Complete qualified table name after dbname.schemaname. -->
    <completion-snapshot name="dots_qualified_table">
        <editor>
            <input>
                create table dbname.schemaname.foo(a int);
                select * from dbname.schemaname.
            </input>
        </editor>
        <cursor>
            <search text="from dbname.schemaname." index="23" />
        </cursor>
        <completions limit="3" />
    </completion-snapshot>
    <!-- Complete column after alias with qualified table (f.) -->
    <completion-snapshot name="dots_column_0">
        <editor>
            <input>
                create table dbname.schemaname.foo(a int);
                select from dbname.schemaname.foo f where f.
            </input>
        </editor>
        <cursor>
            <search text="f." index="2" />
        </cursor>
        <completions limit="3" />
    </completion-snapshot>
    <!-- Complete column with schema-qualified table (default db) -->
    <completion-snapshot name="dots_column_1">
        <editor>
            <input>
                create table schemaname.foo(a int);
                select from schemaname.foo f where f.
            </input>
        </editor>
        <cursor>
            <search text="f." index="2" />
        </cursor>
        <completions limit="3" />
    </completion-snapshot>
    <!-- Complete column after unqualified table alias (f.) -->
    <completion-snapshot name="dots_column_2">
        <editor>
            <input>
                create table foo(a int);
                select from foo f where f.
            </input>
        </editor>
        <cursor>
            <search text="f." index="2" />
        </cursor>
        <completions limit="3" />
    </completion-snapshot>
    <!-- Complete multiple columns for single table alias -->
    <completion-snapshot name="dots_multiple_columns_0">
        <editor>
            <input>
                create table foo(a int, b int, c int);
                create table bar(d int, e int, f int);
                select from foo alias where alias.
            </input>
        </editor>
        <cursor>
            <search text="alias." index="6" />
        </cursor>
        <completions limit="3" />
    </completion-snapshot>
    <!-- Complete columns for alias f with multiple FROMs -->
    <completion-snapshot name="dots_multiple_columns_1">
        <editor>
            <input>
                create table foo(a int, b int, c int);
                create table bar(d int, e int, f int);
                select from foo f, bar b where f.
            </input>
        </editor>
        <cursor>
            <search text="f." index="2" />
        </cursor>
        <completions limit="3" />
    </completion-snapshot>
    <!-- Complete columns for alias b (second table) -->
    <completion-snapshot name="dots_multiple_columns_2">
        <editor>
            <input>
                create table foo(a int, b int, c int);
                create table bar(d int, e int, f int);
                select from foo f, bar b where b.
            </input>
        </editor>
        <cursor>
            <search text="b." index="2" />
        </cursor>
        <completions limit="3" />
    </completion-snapshot>
    <!-- Completion limit: more columns than limit=3 -->
    <completion-snapshot name="dots_limit">
        <editor>
            <input>
                create table foo(a int, b int, c int, d int, e int);
                select from foo f where f.
            </input>
        </editor>
        <cursor>
            <search text="f." index="2" />
        </cursor>
        <completions limit="3" />
    </completion-snapshot>
    <!-- Column names with matching prefix should score higher (f.ap -> apple) -->
    <completion-snapshot name="dots_score_column_prefix_0">
        <editor>
            <input>
                create table foo(apple int, juice int);
                select from foo f where f.ap
            </input>
        </editor>
        <cursor>
            <search text="f.ap" index="4" />
        </cursor>
        <completions limit="3" />
    </completion-snapshot>
</completion-snapshots>
