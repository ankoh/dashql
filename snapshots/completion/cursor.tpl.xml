<completion-snapshots>
    <!-- Cursor after completed word "select " -->
    <completion-snapshot name="completed_word">
        <catalog />
        <editor>
            <input>
                select 
            </input>
        </editor>
        <cursor>
            <search text="select " index="7" />
        </cursor>
        <completions limit="3" />
    </completion-snapshot>
</completion-snapshots>
