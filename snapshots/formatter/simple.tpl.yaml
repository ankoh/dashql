formatter-snapshots:
  # Single literal in select list
  - name: select_1
    input: select 1
    dialects:
      duckdb:
        formatted:
        - mode: compact
  # Multiple literals in select list
  - name: select_1_2
    input: select 1,2
    dialects:
      duckdb:
        formatted:
        - mode: compact
