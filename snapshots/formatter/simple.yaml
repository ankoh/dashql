formatter-snapshots:

  - name: select_1
    input: select 1
    formatted:
      - mode: compact
        expected: |-
          select 1

  - name: select_1_2
    input: select 1,2
    formatted:
      - mode: compact
        expected: |-
          select 1, 2
