formatter-snapshots:

  - name: expr_plus
    input: |-
      select 1+2
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select 1 + 2;
          - mode: compact
            expected: |-
              select 1 + 2;
          - mode: pretty
            expected: |-
              select 1 + 2;

  - name: expr_minus
    input: |-
      select 5-3
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select 5 - 3;
          - mode: compact
            expected: |-
              select 5 - 3;
          - mode: pretty
            expected: |-
              select 5 - 3;

  - name: expr_multiply
    input: |-
      select 3*4
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select 3 * 4;
          - mode: compact
            expected: |-
              select 3 * 4;
          - mode: pretty
            expected: |-
              select 3 * 4;

  - name: expr_divide
    input: |-
      select 10/2
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select 10 / 2;
          - mode: compact
            expected: |-
              select 10 / 2;
          - mode: pretty
            expected: |-
              select 10 / 2;

  - name: expr_modulo
    input: |-
      select 10%3
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select 10 % 3;
          - mode: compact
            expected: |-
              select 10 % 3;
          - mode: pretty
            expected: |-
              select 10 % 3;

  - name: expr_xor
    input: |-
      select 5^3
    dialects:
      duckdb:
        skip: true

  - name: expr_negate
    input: |-
      select -1
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select -1;
          - mode: compact
            expected: |-
              select -1;
          - mode: pretty
            expected: |-
              select -1;

  - name: expr_not
    input: |-
      select not a
    dialects:
      duckdb:
        skip: true

  - name: expr_equal
    input: |-
      select a=1
    dialects:
      duckdb:
        skip: true

  - name: expr_not_equal
    input: |-
      select a<>1
    dialects:
      duckdb:
        skip: true

  - name: expr_less_than
    input: |-
      select a<1
    dialects:
      duckdb:
        skip: true

  - name: expr_greater_than
    input: |-
      select a>1
    dialects:
      duckdb:
        skip: true

  - name: expr_less_equal
    input: |-
      select a<=1
    dialects:
      duckdb:
        skip: true

  - name: expr_greater_equal
    input: |-
      select a>=1
    dialects:
      duckdb:
        skip: true

  - name: expr_and
    input: |-
      select a and b
    dialects:
      duckdb:
        skip: true

  - name: expr_or
    input: |-
      select a or b
    dialects:
      duckdb:
        skip: true

  - name: expr_prec_mul_over_add
    input: |-
      select 1+2*3
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select 1 + 2 * 3;
          - mode: compact
            expected: |-
              select 1 + 2 * 3;
          - mode: pretty
            expected: |-
              select 1 + 2 * 3;

  - name: expr_prec_parens_add_then_mul
    input: |-
      select (1+2)*3
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select (1 + 2) * 3;
          - mode: compact
            expected: |-
              select (1 + 2) * 3;
          - mode: pretty
            expected: |-
              select (1 + 2) * 3;

  - name: expr_prec_negate_sum
    input: |-
      select -(1+2)
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select -(1 + 2);
          - mode: compact
            expected: |-
              select -(1 + 2);
          - mode: pretty
            expected: |-
              select -(1 + 2);

  - name: expr_prec_and_over_or
    input: |-
      select a or b and c from test
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select a or b and c from test;
          - mode: compact
            expected: |-
              select a or b and c from test;
          - mode: pretty
            expected: |-
              select a or b and c
              from test;
        validation:
          setup: |-
            create table test (a int, b int, c int);

  - name: expr_prec_or_inside_and
    input: |-
      select (a or b) and c from test
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select (a or b) and c from test;
          - mode: compact
            expected: |-
              select (a or b) and c from test;
          - mode: pretty
            expected: |-
              select (a or b) and c
              from test;
        validation:
          setup: |-
            create table test (a int, b int, c int);

  - name: expr_prec_not_over_and
    input: |-
      select not a and b from test
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select not a and b from test;
          - mode: compact
            expected: |-
              select not a and b from test;
          - mode: pretty
            expected: |-
              select not a and b
              from test;
        validation:
          setup: |-
            create table test (a int, b int, c int);

  - name: expr_prec_sub_assoc
    input: |-
      select a-(b-c) from test
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select a - (b - c) from test;
          - mode: compact
            expected: |-
              select a - (b - c) from test;
          - mode: pretty
            expected: |-
              select a - (b - c)
              from test;
        validation:
          setup: |-
            create table test (a int, b int, c int);

  - name: expr_like
    input: |-
      select a like 'foo' from test
    dialects:
      duckdb:
        skip: true

  - name: expr_not_like
    input: |-
      select a not like 'foo' from test
    dialects:
      duckdb:
        skip: true

  - name: expr_ilike
    input: |-
      select a ilike b
    dialects:
      duckdb:
        skip: true

  - name: expr_not_ilike
    input: |-
      select a not ilike b
    dialects:
      duckdb:
        skip: true

  - name: expr_result_target_star
    input: |-
      select * from test
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select * from test;
          - mode: compact
            expected: |-
              select * from test;
          - mode: pretty
            expected: |-
              select *
              from test;
        validation:
          setup: |-
            create table test (a int);

  - name: expr_result_target_alias_as
    input: |-
      select a + b as total from test
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select a + b as total from test;
          - mode: compact
            expected: |-
              select a + b as total from test;
          - mode: pretty
            expected: |-
              select a + b as total
              from test;
        validation:
          setup: |-
            create table test (a int, b int);

  - name: expr_result_target_alias_implicit
    input: |-
      select a + b total from test
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select a + b as total from test;
          - mode: compact
            expected: |-
              select a + b as total from test;
          - mode: pretty
            expected: |-
              select a + b as total
              from test;
        validation:
          setup: |-
            create table test (a int, b int);

  - name: expr_function_call_min
    input: |-
      select min(ps_supplycost) from test
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select min(ps_supplycost) from test;
          - mode: compact
            expected: |-
              select min(ps_supplycost) from test;
          - mode: pretty
            expected: |-
              select min(ps_supplycost)
              from test;
        validation:
          setup: |-
            create table test (ps_supplycost int);

  - name: expr_const_function_cast
    input: |-
      select somefunc 'foo'
    dialects:
      duckdb:
        skip: true

  - name: expr_const_function_cast_date
    input: |-
      select date '2021-01-10'
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select date '2021-01-10';
          - mode: compact
            expected: |-
              select date '2021-01-10';
          - mode: pretty
            expected: |-
              select date '2021-01-10';

  - name: expr_const_function_cast_args
    input: |-
      select somefunc(1, 2) 'foo'
    dialects:
      duckdb:
        skip: true

  - name: expr_const_interval_cast
    input: |-
      select interval '30 days'
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select interval '30 days';
          - mode: compact
            expected: |-
              select interval '30 days';
          - mode: pretty
            expected: |-
              select interval '30 days';

  - name: expr_const_interval_cast_typed
    input: |-
      select interval '90' day
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select interval '90' day;
          - mode: compact
            expected: |-
              select interval '90' day;
          - mode: pretty
            expected: |-
              select interval '90' day;

  - name: expr_glob
    input: |-
      select a glob b
    dialects:
      duckdb:
        skip: true

  - name: expr_not_glob
    input: |-
      select a not glob b
    dialects:
      duckdb:
        skip: true

  - name: expr_similar_to
    input: |-
      select a similar to b
    dialects:
      duckdb:
        skip: true

  - name: expr_not_similar_to
    input: |-
      select a not similar to b
    dialects:
      duckdb:
        skip: true

  - name: expr_is_null
    input: |-
      select a is null
    dialects:
      duckdb:
        skip: true

  - name: expr_is_not_null
    input: |-
      select a is not null
    dialects:
      duckdb:
        skip: true

  - name: expr_is_true
    input: |-
      select a is true
    dialects:
      duckdb:
        skip: true

  - name: expr_is_not_true
    input: |-
      select a is not true
    dialects:
      duckdb:
        skip: true

  - name: expr_is_false
    input: |-
      select a is false
    dialects:
      duckdb:
        skip: true

  - name: expr_is_not_false
    input: |-
      select a is not false
    dialects:
      duckdb:
        skip: true

  - name: expr_is_unknown
    input: |-
      select a is unknown
    dialects:
      duckdb:
        skip: true

  - name: expr_is_not_unknown
    input: |-
      select a is not unknown
    dialects:
      duckdb:
        skip: true

  - name: expr_is_distinct_from
    input: |-
      select a is distinct from b
    dialects:
      duckdb:
        skip: true

  - name: expr_is_not_distinct_from
    input: |-
      select a is not distinct from b
    dialects:
      duckdb:
        skip: true

  - name: expr_between
    input: |-
      select a between 1 and 10
    dialects:
      duckdb:
        skip: true

  - name: expr_not_between
    input: |-
      select a not between 1 and 10
    dialects:
      duckdb:
        skip: true

  - name: expr_between_symmetric
    input: |-
      select a between symmetric 1 and 10
    dialects:
      duckdb:
        skip: true

  - name: expr_not_between_symmetric
    input: |-
      select a not between symmetric 1 and 10
    dialects:
      duckdb:
        skip: true

  - name: expr_in_list
    input: |-
      select a in (1, 2, 3)
    dialects:
      duckdb:
        skip: true

  - name: expr_not_in_list
    input: |-
      select a not in (1, 2, 3)
    dialects:
      duckdb:
        skip: true

  - name: expr_and_chain_long
    input: |-
      select very_long_column_name_a = 1 and very_long_column_name_b = 2 and very_long_column_name_c = 3
    dialects:
      duckdb:
        skip: true

  - name: expr_or_chain_long
    input: |-
      select very_long_column_name_a = 1 or very_long_column_name_b = 2 or very_long_column_name_c = 3
    dialects:
      duckdb:
        skip: true

  - name: expr_add_chain_long
    input: |-
      select very_long_column_name_a + very_long_column_name_b + very_long_column_name_c + very_long_column_name_d
    dialects:
      duckdb:
        skip: true

  - name: expr_nested_boolean_chain_long
    input: |-
      select * from test where very_long_outer_left_column = 1 and (very_long_inner_left_column = 2 or very_long_inner_middle_column = 3 or very_long_inner_right_column = 4) and very_long_outer_right_column = 5
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select * from test where very_long_outer_left_column = 1 and (very_long_inner_left_column = 2 or very_long_inner_middle_column = 3 or very_long_inner_right_column = 4) and very_long_outer_right_column = 5;
          - mode: compact
            width: 80
            expected: |-
              select *
              from test
              where very_long_outer_left_column = 1 and (
                  very_long_inner_left_column = 2 or very_long_inner_middle_column = 3
                    or very_long_inner_right_column = 4
                ) and very_long_outer_right_column = 5;
          - mode: pretty
            width: 80
            expected: |-
              select *
              from test
              where very_long_outer_left_column = 1
                and (
                  very_long_inner_left_column = 2
                    or very_long_inner_middle_column = 3
                    or very_long_inner_right_column = 4
                )
                and very_long_outer_right_column = 5;

  - name: expr_typecast_int
    input: |-
      select x::integer
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select x::integer;
          - mode: compact
            expected: |-
              select x::integer;
          - mode: pretty
            expected: |-
              select x::integer;

  - name: expr_typecast_varchar
    input: |-
      select 123::varchar
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select 123::varchar;
          - mode: compact
            expected: |-
              select 123::varchar;
          - mode: pretty
            expected: |-
              select 123::varchar;

  - name: expr_typecast_chain
    input: |-
      select x::integer::varchar
    dialects:
      duckdb:
        skip: true

  - name: expr_exists
    input: |-
      select exists (select 1)
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select exists (select 1);
          - mode: compact
            expected: |-
              select exists (select 1);
          - mode: pretty
            expected: |-
              select exists (select 1);

  - name: expr_case_simple
    input: |-
      select case a when 1 then 'one' when 2 then 'two' end
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select case a when 1 then 'one' when 2 then 'two' end;
          - mode: compact
            expected: |-
              select case a when 1 then 'one' when 2 then 'two' end;
          - mode: pretty
            expected: |-
              select case a when 1 then 'one' when 2 then 'two' end;

  - name: expr_case_simple_else
    input: |-
      select case a when 1 then 'one' else 'other' end
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select case a when 1 then 'one' else 'other' end;
          - mode: compact
            expected: |-
              select case a when 1 then 'one' else 'other' end;
          - mode: pretty
            expected: |-
              select case a when 1 then 'one' else 'other' end;

  - name: expr_case_searched
    input: |-
      select case when a > 0 then 'pos' when a < 0 then 'neg' else 'zero' end
    dialects:
      duckdb:
        skip: true

  - name: expr_case_long
    input: |-
      select case very_long_column_name when 'very_long_value_one' then 'result_one' when 'very_long_value_two' then 'result_two' when 'very_long_value_three' then 'result_three' else 'default_result' end
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select case very_long_column_name when 'very_long_value_one' then 'result_one' when 'very_long_value_two' then 'result_two' when 'very_long_value_three' then 'result_three' else 'default_result' end;
          - mode: compact
            width: 80
            expected: |-
              select case very_long_column_name when 'very_long_value_one' then 'result_one'
                when 'very_long_value_two' then 'result_two'
                when 'very_long_value_three' then 'result_three' else 'default_result' end;
          - mode: pretty
            width: 80
            expected: |-
              select case very_long_column_name when 'very_long_value_one' then 'result_one'
                when 'very_long_value_two' then 'result_two'
                when 'very_long_value_three' then 'result_three' else 'default_result' end;

  - name: expr_union
    input: |-
      select 1 union select 2
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select 1 union select 2;
          - mode: compact
            expected: |-
              select 1 union select 2;
          - mode: pretty
            expected: |-
              select 1 union select 2;

  - name: expr_union_all
    input: |-
      select 1 union all select 2
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select 1 union all select 2;
          - mode: compact
            expected: |-
              select 1 union all select 2;
          - mode: pretty
            expected: |-
              select 1 union all select 2;

  - name: expr_intersect
    input: |-
      select 1 intersect select 2
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select 1 intersect select 2;
          - mode: compact
            expected: |-
              select 1 intersect select 2;
          - mode: pretty
            expected: |-
              select 1 intersect select 2;

  - name: expr_except
    input: |-
      select 1 except select 2
    dialects:
      duckdb:
        formatted:
          - mode: inline
            expected: |-
              select 1 except select 2;
          - mode: compact
            expected: |-
              select 1 except select 2;
          - mode: pretty
            expected: |-
              select 1 except select 2;
