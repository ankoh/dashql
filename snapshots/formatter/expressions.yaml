formatter-snapshots:

  - name: expr_plus
    input: select 1+2
    dialects:
      duckdb:
        formatted:
          - mode: compact
            expected: |-
              select 1 + 2
        validation: 
