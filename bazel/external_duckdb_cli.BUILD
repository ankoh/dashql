# DuckDB: prebuilt CLI executable. One archive per exec platform, selected via the
# //bazel:duckdb_cli alias. Used as an exec-config build tool (Parquet conversion).
# The release zip contains a single `duckdb` executable with the exec bit preserved.

package(default_visibility = ["//visibility:public"])

exports_files(["duckdb"])
