diff --git a/include/codegen/BUILD.bazel b/include/codegen/BUILD.bazel
index 1234567..abcdefg 100644
--- a/include/codegen/BUILD.bazel
+++ b/include/codegen/BUILD.bazel
@@ -24,6 +24,14 @@ cc_library(
     strip_include_prefix = "/include",
     visibility = ["//:__subpackages__"],
     deps = ["//:runtime_cc"],
+    copts = select({
+        "@platforms//cpu:wasm32": [
+            "-pthread",
+            "-matomics",
+            "-mbulk-memory",
+        ],
+        "//conditions:default": [],
+    }),
 )

 cc_library(
@@ -36,4 +44,12 @@ cc_library(
         "//src:__subpackages__",
     ],
     deps = [":namer"],
+    copts = select({
+        "@platforms//cpu:wasm32": [
+            "-pthread",
+            "-matomics",
+            "-mbulk-memory",
+        ],
+        "//conditions:default": [],
+    }),
 )
diff --git a/src/BUILD.bazel b/src/BUILD.bazel
index 2345678..bcdefgh 100644
--- a/src/BUILD.bazel
+++ b/src/BUILD.bazel
@@ -24,6 +24,14 @@ cc_library(
     ],
     strip_include_prefix = "/include",
     visibility = ["//:__subpackages__"],
+    copts = select({
+        "@platforms//cpu:wasm32": [
+            "-pthread",
+            "-matomics",
+            "-mbulk-memory",
+        ],
+        "//conditions:default": [],
+    }),
 )

 cc_library(
@@ -33,6 +41,14 @@ cc_library(
     strip_include_prefix = "/src",
     visibility = ["//:__subpackages__"],
     deps = [":code_generators"],
+    copts = select({
+        "@platforms//cpu:wasm32": [
+            "-pthread",
+            "-matomics",
+            "-mbulk-memory",
+        ],
+        "//conditions:default": [],
+    }),
 )

 # Public flatc library to compile flatbuffer files at runtime.
@@ -59,6 +75,14 @@ cc_library(
     }),
     strip_include_prefix = "/include",
     visibility = ["//:__subpackages__"],
+    copts = select({
+        "@platforms//cpu:wasm32": [
+            "-pthread",
+            "-matomics",
+            "-mbulk-memory",
+        ],
+        "//conditions:default": [],
+    }),
     deps = [
         ":code_generators",
         ":generate_fbs",
@@ -87,6 +111,14 @@ cc_library(
     ],
     strip_include_prefix = "/include",
     visibility = ["//:__pkg__"],
+    copts = select({
+        "@platforms//cpu:wasm32": [
+            "-pthread",
+            "-matomics",
+            "-mbulk-memory",
+        ],
+        "//conditions:default": [],
+    }),
     deps = [
         ":flatbuffers",
         "//include/codegen:namer",
@@ -147,6 +179,14 @@ cc_library(
     ],
     strip_include_prefix = "/include",
     visibility = ["//:__pkg__"],
+    copts = select({
+        "@platforms//cpu:wasm32": [
+            "-pthread",
+            "-matomics",
+            "-mbulk-memory",
+        ],
+        "//conditions:default": [],
+    }),
     deps = [
         ":flatc_library",
         "//grpc/src/compiler:cpp_generator",
