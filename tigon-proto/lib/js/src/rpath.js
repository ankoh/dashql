// Automatically generated by the Fast Binary Encoding compiler, do not modify!
// https://github.com/chronoxor/FastBinaryEncoding
// Source: rpath.fbe
// Version: 1.3.0.0

/* eslint-disable prefer-const */
'use strict'

const util = require('util')

const big = require('./big')
const int64 = require('./int64')
const uuid = require('./uuid')

const Big = big.Big // eslint-disable-line
const Int64 = int64.Int64 // eslint-disable-line
const UInt64 = int64.UInt64 // eslint-disable-line
const UUID = uuid.UUID // eslint-disable-line

const fbe = require('./fbe')

/**
 * RPathArraySlice struct
 */
class RPathArraySlice {
  /**
   * Copy struct (shallow copy)
   * @this {!RPathArraySlice}
   * @param {!RPathArraySlice} other Other struct
   * @returns {!RPathArraySlice} This struct
   */
  copy (other) {
    return this
  }

  /**
   * Clone struct (deep clone)
   * @this {!RPathArraySlice}
   * @returns {!RPathArraySlice} Cloned struct
   */
  clone () {
    // Serialize the struct to the FBE stream
    let writer = new RPathArraySliceModel(new fbe.WriteBuffer())
    writer.serialize(this)

    // Deserialize the struct from the FBE stream
    let reader = new RPathArraySliceModel(new fbe.ReadBuffer())
    reader.attachBuffer(writer.buffer)
    return reader.deserialize().value
  }

  /**
   * Is this struct equal to other one?
   * @this {!RPathArraySlice}
   * @param {!RPathArraySlice} other Other struct
   * @returns {boolean} Equal result
   */
  eq (other) {
    if (!(other instanceof RPathArraySlice)) {
      throw new TypeError('Instance of RPathArraySlice is required!')
    }
    return true
  }

  /**
   * Convert struct to string
   * @this {!RPathArraySlice}
   * @returns {!string} Struct value string
   */
  toString () {
    let result = ''
    result += 'RPathArraySlice('
    result += ')'
    return result
  }

  /**
   * Inspect struct
   * @this {!RPathArraySlice}
   * @returns {!string} Struct value string
   */
  [util.inspect.custom] () {
    return this.toString()
  }

  /**
   * Convert struct to JSON
   * @this {!RPathArraySlice}
   * @returns {!object} Struct value for JSON
   */
  toJSON () {
    return {
    }
  }

  /**
   * Convert JSON to struct
   * @param {!string} json JSON string
   * @returns {!object} Struct value for JSON
   */
  static fromJSON (json) {
    return RPathArraySlice.fromObject(JSON.parse(json))
  }

  /**
   * Create struct from object value
   * @param {!RPathArraySlice} other Object value
   * @returns {!RPathArraySlice} Created struct
   */
  static fromObject (other) {
    return new RPathArraySlice().copy(other)
  }

  /**
   * Get the FBE type
   * @this {!RPathArraySlice}
   * @returns {!number} FBE type
   */
  get fbeType () {
    return RPathArraySlice.fbeType
  }

  /**
   * Get the FBE type (static)
   * @this {!RPathArraySlice}
   * @returns {!number} FBE type
   */
  static get fbeType () {
    return 1
  }
}

exports.RPathArraySlice = RPathArraySlice

/**
 * Fast Binary Encoding RPathArraySlice field model
 */
class FieldModelRPathArraySlice extends fbe.FieldModel {
  /**
   * Get the field size
   * @this {!FieldModelRPathArraySlice}
   * @returns {!number} Field size
   */
  get fbeSize () {
    return 4
  }

  /**
   * Get the field body size
   * @this {!FieldModelRPathArraySlice}
   * @returns {!number} Field body size
   */
  get fbeBody () {
    return 4 + 4
  }

  /**
   * Get the field extra size
   * @this {!FieldModelRPathArraySlice}
   * @returns {!number} Field extra size
   */
  get fbeExtra () {
    if ((this._buffer.offset + this.fbeOffset + this.fbeSize) > this._buffer.size) {
      return 0
    }

    let fbeStructOffset = this.readUInt32(this.fbeOffset)
    if ((fbeStructOffset === 0) || ((this._buffer.offset + fbeStructOffset + 4) > this._buffer.size)) {
      return 0
    }

    this._buffer.shift(fbeStructOffset)

    let fbeResult = this.fbeBody

    this._buffer.unshift(fbeStructOffset)

    return fbeResult
  }

  /**
   * Get the field type
   * @this {!FieldModelRPathArraySlice}
   * @returns {!number} Field type
   */
  get fbeType () {
    return FieldModelRPathArraySlice.fbeType
  }

  /**
   * Get the field type (static)
   * @this {!FieldModelRPathArraySlice}
   * @returns {!number} Field type
   */
  static get fbeType () {
    return 1
  }

  /**
   * Check if the struct value is valid
   * @this {!FieldModelRPathArraySlice}
   * @param {!boolean} fbeVerifyType Verify model type flag, defaults is true
   * @returns {!boolean} Field model valid state
   */
  verify (fbeVerifyType = true) {
    if ((this._buffer.offset + this.fbeOffset + this.fbeSize) > this._buffer.size) {
      return true
    }

    let fbeStructOffset = this.readUInt32(this.fbeOffset)
    if ((fbeStructOffset === 0) || ((this._buffer.offset + fbeStructOffset + 4 + 4) > this._buffer.size)) {
      return false
    }

    let fbeStructSize = this.readUInt32(fbeStructOffset)
    if (fbeStructSize < (4 + 4)) {
      return false
    }

    let fbeStructType = this.readUInt32(fbeStructOffset + 4)
    if (fbeVerifyType && (fbeStructType !== this.fbeType)) {
      return false
    }

    this._buffer.shift(fbeStructOffset)
    let fbeResult = this.verifyFields(fbeStructSize)
    this._buffer.unshift(fbeStructOffset)
    return fbeResult
  }

  /**
   * Check if the struct fields are valid
   * @this {!FieldModelRPathArraySlice}
   * @param {!number} fbeStructSize FBE struct size
   * @returns {!boolean} Field model valid state
   */
  verifyFields (fbeStructSize) {
    return true
  }

  /**
   * Get the struct value (begin phase)
   * @this {!FieldModelRPathArraySlice}
   * @returns {!number} Field model begin offset
   */
  getBegin () {
    if ((this._buffer.offset + this.fbeOffset + this.fbeSize) > this._buffer.size) {
      return 0
    }

    let fbeStructOffset = this.readUInt32(this.fbeOffset)
    console.assert((fbeStructOffset > 0) && ((this._buffer.offset + fbeStructOffset + 4 + 4) <= this._buffer.size), 'Model is broken!')
    if ((fbeStructOffset === 0) || ((this._buffer.offset + fbeStructOffset + 4 + 4) > this._buffer.size)) {
      return 0
    }

    let fbeStructSize = this.readUInt32(fbeStructOffset)
    console.assert((fbeStructSize >= (4 + 4)), 'Model is broken!')
    if (fbeStructSize < (4 + 4)) {
      return 0
    }

    this._buffer.shift(fbeStructOffset)
    return fbeStructOffset
  }

  /**
   * Get the struct value (end phase)
   * @this {!FieldModelRPathArraySlice}
   * @param {!number} fbeBegin Field model begin offset
   */
  getEnd (fbeBegin) {
    this._buffer.unshift(fbeBegin)
  }

  /**
   * Get the struct value
   * @this {!FieldModelRPathArraySlice}
   * @param {!RPathArraySlice} fbeValue Default value, defaults is new RPathArraySlice()
   * @returns {!RPathArraySlice} RPathArraySlice value
   */
  get (fbeValue = new RPathArraySlice()) {
    let fbeBegin = this.getBegin()
    if (fbeBegin === 0) {
      return fbeValue
    }

    let fbeStructSize = this.readUInt32(0)
    this.getFields(fbeValue, fbeStructSize)
    this.getEnd(fbeBegin)
    return fbeValue
  }

  /**
   * Get the struct fields values
   * @this {!FieldModelRPathArraySlice}
   * @param {!RPathArraySlice} fbeValue RPathArraySlice value
   * @param {!number} fbeStructSize Struct size
   */
  getFields (fbeValue, fbeStructSize) {
  }

  /**
   * Set the struct value (begin phase)
   * @this {!FieldModelRPathArraySlice}
   * @returns {!number} Field model begin offset
   */
  setBegin () {
    console.assert(((this._buffer.offset + this.fbeOffset + this.fbeSize) <= this._buffer.size), 'Model is broken!')
    if ((this._buffer.offset + this.fbeOffset + this.fbeSize) > this._buffer.size) {
      return 0
    }

    let fbeStructSize = this.fbeBody
    let fbeStructOffset = this._buffer.allocate(fbeStructSize) - this._buffer.offset
    console.assert((fbeStructOffset > 0) && ((this._buffer.offset + fbeStructOffset + fbeStructSize) <= this._buffer.size), 'Model is broken!')
    if ((fbeStructOffset <= 0) || ((this._buffer.offset + fbeStructOffset + fbeStructSize) > this._buffer.size)) {
      return 0
    }

    this.writeUInt32(this.fbeOffset, fbeStructOffset)
    this.writeUInt32(fbeStructOffset, fbeStructSize)
    this.writeUInt32(fbeStructOffset + 4, this.fbeType)

    this._buffer.shift(fbeStructOffset)
    return fbeStructOffset
  }

  /**
   * Set the struct value (end phase)
   * @this {!FieldModelRPathArraySlice}
   * @param {!number} fbeBegin Field model begin offset
   */
  setEnd (fbeBegin) {
    this._buffer.unshift(fbeBegin)
  }

  /**
   * Set the struct value
   * @this {!FieldModelRPathArraySlice}
   * @param {!RPathArraySlice} fbeValue RPathArraySlice value
   */
  set (fbeValue) {
    let fbeBegin = this.setBegin()
    if (fbeBegin === 0) {
      return
    }

    this.setFields(fbeValue)
    this.setEnd(fbeBegin)
  }

  /**
   * Set the struct fields values
   * @this {!FieldModelRPathArraySlice}
   * @param {RPathArraySlice} fbeValue RPathArraySlice value
   */
  setFields (fbeValue) {
  }
}

exports.FieldModelRPathArraySlice = FieldModelRPathArraySlice

/**
 * Fast Binary Encoding RPathArraySlice model
 */
class RPathArraySliceModel extends fbe.Model {
  /**
   * Initialize model with the given buffer
   * @param {!fbe.ReadBuffer|!fbe.WriteBuffer} buffer Read/Write buffer, defaults is new fbe.WriteBuffer()
   * @constructor
   */
  constructor (buffer = new fbe.WriteBuffer()) {
    super(buffer)
    this._model = new FieldModelRPathArraySlice(this.buffer, 4)
  }

  /**
   * Get the RPathArraySlice model
   * @this {!RPathArraySliceModel}
   * @returns {!FieldModelRPathArraySlice} model RPathArraySlice model
   */
  get model () {
    return this._model
  }

  /**
   * Get the model size
   * @this {!RPathArraySliceModel}
   * @returns {!number} Model size
   */
  get fbeSize () {
    return this.model.fbeSize + this.model.fbeExtra
  }

  /**
   * Get the model type
   * @this {!RPathArraySliceModel}
   * @returns {!number} Model type
   */
  get fbeType () {
    return RPathArraySliceModel.fbeType
  }

  /**
   * Get the model type (static)
   * @this {!RPathArraySliceModel}
   * @returns {!number} Model type
   */
  static get fbeType () {
    return FieldModelRPathArraySlice.fbeType
  }

  /**
   * Check if the struct value is valid
   * @this {!RPathArraySliceModel}
   * @returns {!boolean} Model valid state
   */
  verify () {
    if ((this.buffer.offset + this.model.fbeOffset - 4) > this.buffer.size) {
      return false
    }

    let fbeFullSize = this.readUInt32(this.model.fbeOffset - 4)
    if (fbeFullSize < this.model.fbeSize) {
      return false
    }

    return this.model.verify()
  }

  /**
   * Create a new model (begin phase)
   * @this {!RPathArraySliceModel}
   * @returns {!number} Model begin offset
   */
  createBegin () {
    return this.buffer.allocate(4 + this.model.fbeSize)
  }

  /**
   * Create a new model (end phase)
   * @this {!RPathArraySliceModel}
   * @param {!number} fbeBegin Model begin offset
   */
  createEnd (fbeBegin) {
    let fbeEnd = this.buffer.size
    let fbeFullSize = fbeEnd - fbeBegin
    this.writeUInt32(this.model.fbeOffset - 4, fbeFullSize)
    return fbeFullSize
  }

  /**
   * Serialize the struct value
   * @this {!RPathArraySliceModel}
   * @param {!RPathArraySlice} value RPathArraySlice value
   * @return {!number} Model begin offset
   */
  serialize (value) {
    let fbeBegin = this.createBegin()
    this.model.set(value)
    return this.createEnd(fbeBegin)
  }

  /**
   * Deserialize the struct value
   * @this {!RPathArraySliceModel}
   * @param {!RPathArraySlice} value RPathArraySlice value, defaults is new RPathArraySlice()
   * @return {!object} Deserialized RPathArraySlice value and its size
   */
  deserialize (value = new RPathArraySlice()) {
    if ((this.buffer.offset + this.model.fbeOffset - 4) > this.buffer.size) {
      return { value: new RPathArraySlice(), size: 0 }
    }

    let fbeFullSize = this.readUInt32(this.model.fbeOffset - 4)
    console.assert((fbeFullSize >= this.model.fbeSize), 'Model is broken!')
    if (fbeFullSize < this.model.fbeSize) {
      return { value: new RPathArraySlice(), size: 0 }
    }

    this.model.get(value)
    return { value: value, size: fbeFullSize }
  }

  /**
   * Move to the next struct value
   * @this {!RPathArraySliceModel}
   * @param {!number} prev Previous RPathArraySlice model size
   */
  next (prev) {
    this.model.fbeShift(prev)
  }
}

exports.RPathArraySliceModel = RPathArraySliceModel
