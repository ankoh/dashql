// Automatically generated by the Fast Binary Encoding compiler, do not modify!
// https://github.com/chronoxor/FastBinaryEncoding
// Source: tql.fbe
// Version: 1.3.0.0

/* eslint-disable prefer-const */
'use strict'

const util = require('util')

const big = require('./big')
const int64 = require('./int64')
const uuid = require('./uuid')

const Big = big.Big // eslint-disable-line
const Int64 = int64.Int64 // eslint-disable-line
const UInt64 = int64.UInt64 // eslint-disable-line
const UUID = uuid.UUID // eslint-disable-line

const fbe = require('./fbe')

/**
 * TQLType enum
 */
class TQLType {
  /**
   * Initialize enum with a given value
   * @param {TQLType|number|Int64|UInt64=} value Enum value, defaults is 0
   * @constructor
   */
  constructor (value = 0) {
    if (value instanceof TQLType) {
      this.value = value.value
    } else {
      this.value = value
    }
  }

  /**
   * Is this enum equal to other one?
   * @this {!TQLType}
   * @param {!TQLType} other Other enum
   * @returns {boolean} Equal result
   */
  eq (other) {
    if (!(other instanceof TQLType)) {
      throw new TypeError('Instance of TQLType is required!')
    }
    return this.value === other.value
  }

  /**
   * Get enum value
   * @this {!TQLType}
   * @returns {!number|!Int64|!UInt64} Enum value
   */
  valueOf () {
    return this.value
  }

  /**
   * Convert enum to string
   * @this {!TQLType}
   * @returns {!string} Enum value string
   */
  toString () {
    if (this.value === TQLType.INTEGER.value) {
      return 'INTEGER'
    }
    if (this.value === TQLType.FLOAT.value) {
      return 'FLOAT'
    }
    if (this.value === TQLType.TEXT.value) {
      return 'TEXT'
    }
    if (this.value === TQLType.DATE.value) {
      return 'DATE'
    }
    if (this.value === TQLType.DATETIME.value) {
      return 'DATETIME'
    }
    if (this.value === TQLType.TIME.value) {
      return 'TIME'
    }
    return '<unknown>'
  }

  /**
   * Inspect enum
   * @this {!TQLType}
   * @returns {!string} Enum value string
   */
  [util.inspect.custom] () {
    return this.toString()
  }

  /**
   * Convert enum to JSON
   * @this {!TQLType}
   * @returns {!number} Enum value for JSON
   */
  toJSON () {
    return this.value
  }

  /**
   * Create enum from object value
   * @param {!number} other Object value
   * @returns {!TQLType} Created enum
   */
  static fromObject (other) {
    return new TQLType(other)
  }
}

// noinspection PointlessArithmeticExpressionJS
TQLType.INTEGER = new TQLType(0 + 0)
// noinspection PointlessArithmeticExpressionJS
TQLType.FLOAT = new TQLType(1 + 0)
// noinspection PointlessArithmeticExpressionJS
TQLType.TEXT = new TQLType(2 + 0)
// noinspection PointlessArithmeticExpressionJS
TQLType.DATE = new TQLType(3 + 0)
// noinspection PointlessArithmeticExpressionJS
TQLType.DATETIME = new TQLType(4 + 0)
// noinspection PointlessArithmeticExpressionJS
TQLType.TIME = new TQLType(5 + 0)

exports.TQLType = TQLType

/**
 * Fast Binary Encoding TQLType field model
 */
class FieldModelTQLType extends fbe.FieldModel {
  /**
   * Get the field size
   * @this {!FieldModelTQLType}
   * @returns {!number} Field size
   */
  get fbeSize () {
    return 1
  }

  /**
   * Get the value
   * @this {!FieldModelTQLType}
   * @param {TQLType=} defaults Default value, defaults is new TQLType()
   * @returns {!TQLType} Result value
   */
  get (defaults = new TQLType()) {
    if ((this._buffer.offset + this.fbeOffset + this.fbeSize) > this._buffer.size) {
      return defaults
    }

    return new TQLType(this.readByte(this.fbeOffset))
  }

  /**
   * Set the value
   * @this {!FieldModelTQLType}
   * @param {!TQLType} value Value
   */
  set (value) {
    console.assert(((this._buffer.offset + this.fbeOffset + this.fbeSize) <= this._buffer.size), 'Model is broken!')
    if ((this._buffer.offset + this.fbeOffset + this.fbeSize) > this._buffer.size) {
      return
    }

    this.writeByte(this.fbeOffset, value.value)
  }
}

exports.FieldModelTQLType = FieldModelTQLType
