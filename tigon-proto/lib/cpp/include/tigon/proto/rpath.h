// Automatically generated by the Fast Binary Encoding compiler, do not modify!
// https://github.com/chronoxor/FastBinaryEncoding
// Source: rpath.fbe
// Version: 1.3.0.0

#pragma once

#if defined(__clang__)
#pragma clang system_header
#elif defined(__GNUC__)
#pragma GCC system_header
#elif defined(_MSC_VER)
#pragma system_header
#endif

#include "fbe.h"

namespace rpath {
using namespace FBE;
} // namespace rpath

namespace FBE {
using namespace ::rpath;
} // namespace FBE

namespace rpath {

struct RPathArraySlice
{

    size_t fbe_type() const noexcept { return 1; }

    RPathArraySlice()
    {}
    RPathArraySlice(const RPathArraySlice& other) = default;
    RPathArraySlice(RPathArraySlice&& other) = default;
    ~RPathArraySlice() = default;

    RPathArraySlice& operator=(const RPathArraySlice& other) = default;
    RPathArraySlice& operator=(RPathArraySlice&& other) = default;

    bool operator==(const RPathArraySlice& other) const noexcept
    {
        return (
            true
            );
    }
    bool operator!=(const RPathArraySlice& other) const noexcept { return !operator==(other); }
    bool operator<(const RPathArraySlice& other) const noexcept
    {
        return false;
    }
    bool operator<=(const RPathArraySlice& other) const noexcept { return operator<(other) || operator==(other); }
    bool operator>(const RPathArraySlice& other) const noexcept { return !operator<=(other); }
    bool operator>=(const RPathArraySlice& other) const noexcept { return !operator<(other); }

    std::string string() const
    {
        std::stringstream ss;
        ss << *this;
        return ss.str();
    }

    template <class TOutputStream>
    friend TOutputStream& operator<<(TOutputStream& stream, const RPathArraySlice& value);

    void swap(RPathArraySlice& other) noexcept
    {
        using std::swap;
    }

    friend void swap(RPathArraySlice& value1, RPathArraySlice& value2) noexcept
    {
        value1.swap(value2);
    }
};

template <class TOutputStream>
inline TOutputStream& operator<<(TOutputStream& stream, const RPathArraySlice& value)
{
    stream << "RPathArraySlice(";
    stream << ")";
    return stream;
}

} // namespace rpath

namespace std {

template<>
struct hash<rpath::RPathArraySlice>
{
    typedef rpath::RPathArraySlice argument_type;
    typedef size_t result_type;

    result_type operator () (const argument_type& value) const
    {
        result_type result = 17;
        return result;
    }
};

} // namespace std
