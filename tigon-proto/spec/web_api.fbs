namespace tigon.proto;

// A status code
enum StatusCode: uint8 {
    SUCCESS = 0,
    ERROR = 1,
}

/// A formatted text
table FormattedText {
    text: string;
}

/// A raw data buffer
table RawData {
    data: [ubyte];
}
