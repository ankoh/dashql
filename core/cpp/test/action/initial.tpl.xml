<test name="parameter_change">
    <previous>
        <text>
            DECLARE PARAMETER country TYPE TEXT (
                default_value = 'DE'
            );
            LOAD weather_csv FROM http (
                url = format('https://cdn.dashql.com/demo/weather/%s', global.country)
            );
        </text>
        <graph>
            <program>
                <action status="COMPLETED" />
                <action status="NONE" />
            </program>
        </graph>
    </previous>
    <next>
        <text>
            DECLARE PARAMETER country TYPE TEXT (
                default_value = 'DE'
            );
            LOAD weather_csv FROM http (
                url = format('https://cdn.dashql.com/demo/weather/%s', global.country)
            );
        </text>
    </next>
</test>
