[package]
name = "dashql-core"
version = "0.0.0"
authors = ["The DashQL Authors"]
edition = "2018"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2.68"
dashql-parser = { path = "../../dashql-parser/libs/rs" }
flatbuffers = "0.7.0"
