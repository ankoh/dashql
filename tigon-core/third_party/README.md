# Third-party dependencies

Updating the dependencies is a little bit tedious.
We need to maintain all dependencies on our own since the projects are not compatible with the emscripten toolchain.

Arrow dependencies:
Boost, Brotli, DoubleConversion, Snappy, Thrift, Flatbuffers
