# BUILD file for ankerl/unordered_dense (header-only).

load("@rules_cc//cc:defs.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "ankerl",
    hdrs = glob(["include/**/*.h"], allow_empty = False),
    includes = ["include"],
)
