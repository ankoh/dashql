# frozen - in-tree vendor (header-only).

load("@rules_cc//cc:defs.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "frozen",
    hdrs = glob(["include/**/*.h"]),
    includes = ["include"],
)
